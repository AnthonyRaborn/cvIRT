% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mirt_adapter.R
\name{fit_irt_model}
\alias{fit_irt_model}
\title{Fit a single IRT model via mirt, returning a standardized result list.}
\usage{
fit_irt_model(data, model_type, fixed_pars = NULL, verbose = FALSE, ...)
}
\arguments{
\item{data}{Response matrix (persons x items).}

\item{model_type}{One of `cvirt_valid_models`.}

\item{fixed_pars}{`NULL` for free estimation, or a fixed-parameter table
from `extract_fixed_pars()` to fit on new data with item parameters held
at previously estimated values (holdout/k-fold/LOOCV test fits).}

\item{verbose}{Passed through to `mirt::mirt(verbose = ...)`.}

\item{...}{Additional arguments forwarded to `mirt::mirt()`.}
}
\value{
A list with `fit` (the mirt object), `loglik`, `npar`, and
  `fixed_pars` (ready to pass to a subsequent `fit_irt_model()` call).
}
\description{
This is the single entry point every cvIRT CV method calls instead of
`TAM::` directly, isolating the mirt dependency to this file.
}
\keyword{internal}
